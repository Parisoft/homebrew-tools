# Build progress guardrail

This `wip-build` branch is a scratch branch used only to keep a safe,
pushed copy of the ongoing build state in case the sandbox resets. It is
NOT a delivery branch.

Status (rebuilt after sandbox reset on 2026-08-29):

- [x] mame-konami  (pushed to `mame-konami`)
- [x] mame-capcom  (pushed to `mame-capcom`)
- [ ] mame-sega    (building)
- [ ] mame-taito
- [ ] mame-tecmo
- [ ] mame-technos

Each finished system gets its own `mame-<system>` orphaned branch.
